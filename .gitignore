bin/
tmp/
dist/
# viewer_smoke.mjs's pinned local Playwright install (npm install --no-save).
node_modules/
replay-viewer/dist/nimcache/
*.replay
# Host-specific nimby package paths; regenerate per machine (see README).
nim.cfg
